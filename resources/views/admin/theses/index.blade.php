<x-app-layout><div><p class="text-sm font-bold text-blue-600">ADMINISTRATION</p><h1 class="text-3xl font-black">Thesis management</h1></div><section class="card mt-6"><form class="mb-5 flex flex-wrap gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search title or student"><select name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}">{{ $status->label() }}</option>@endforeach</select><button class="btn-primary">Filter</button></form><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-400"><tr><th class="py-3">Student / thesis</th><th>Status</th><th>Mentor</th><th>Versions</th><th></th></tr></thead><tbody class="divide-y">@foreach($theses as $thesis)<tr><td class="py-4"><b>{{ $thesis->student->name }}</b><p class="max-w-sm truncate text-slate-500">{{ $thesis->title }}</p></td><td><x-status-badge :status="$thesis->status"/></td><td><form method="POST" action="{{ route('admin.theses.assign',$thesis) }}" class="flex gap-2">@csrf @method('PATCH')<select name="professor_id"><option value="">Select mentor</option>@foreach($professors as $professor)<option value="{{ $professor->id }}" @selected($thesis->professor_id===$professor->id)>{{ $professor->name }}</option>@endforeach</select><button class="btn-secondary">Assign</button></form></td><td>{{ $thesis->versions_count }}</td><td><a class="font-bold text-blue-600" href="{{ route('theses.show',$thesis) }}">Open</a></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $theses->links() }}</div></section></x-app-layout>
