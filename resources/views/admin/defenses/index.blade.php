<x-app-layout><div><p class="text-sm font-bold text-violet-600">ADMINISTRATION</p><h1 class="text-3xl font-black">Defense scheduling</h1><p class="mt-2 text-slate-500">Set the date, room, and committee for each defense.</p></div><div class="mt-6 grid gap-6 lg:grid-cols-2">@foreach($theses as $thesis)<section class="card"><div class="flex justify-between gap-3"><div><h2 class="font-black">{{ $thesis->student->name }}</h2><p class="text-sm text-slate-500">{{ $thesis->title }}</p></div><x-status-badge :status="$thesis->status"/></div><form method="POST" action="{{ route('admin.defenses.store',$thesis) }}" class="mt-5 grid gap-3 sm:grid-cols-2">@csrf @method('PUT')<div><label class="label">Date and time</label><input class="w-full" type="datetime-local" name="scheduled_at" value="{{ $thesis->defenseSchedule?->scheduled_at?->format('Y-m-d\TH:i') }}" required></div><div><label class="label">Location</label><input class="w-full" name="location" value="{{ $thesis->defenseSchedule?->location }}" required></div><div><label class="label">Committee member 1</label><input class="w-full" name="committee_members[]" value="{{ $thesis->defenseSchedule?->committee_members[0] ?? '' }}" required></div><div><label class="label">Committee member 2</label><input class="w-full" name="committee_members[]" value="{{ $thesis->defenseSchedule?->committee_members[1] ?? '' }}" required></div><textarea class="sm:col-span-2" name="notes" placeholder="Notes">{{ $thesis->defenseSchedule?->notes }}</textarea><button class="btn-primary sm:col-span-2">Save defense schedule</button></form></section>@endforeach</div></x-app-layout>
