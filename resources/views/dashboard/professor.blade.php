<x-app-layout><div class="mb-8"><p class="text-sm font-bold text-violet-600">MENTOR WORKSPACE</p><h1 class="text-3xl font-black">Professor dashboard</h1><p class="mt-2 text-slate-500">Review submissions and guide students toward defense.</p></div><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><x-metric-card label="Assigned students" :value="$assignedStudents"/><x-metric-card label="Pending reviews" :value="$pendingReviews" color="amber"/><x-metric-card label="Approved theses" :value="$approvedTheses" color="emerald"/><x-metric-card label="Upcoming defenses" :value="$upcomingDefenses" color="violet"/></div><section class="card mt-6"><div class="flex flex-col justify-between gap-4 lg:flex-row lg:items-center"><h2 class="section-title">Assigned students</h2><form class="flex flex-wrap gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search student"><select name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><button class="btn-primary">Filter</button></form></div><div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-slate-400"><tr><th class="py-3">Student</th><th>Thesis</th><th>Phase</th><th>Versions</th><th></th></tr></thead><tbody class="divide-y">@forelse($theses as $thesis)<tr><td class="py-4"><b>{{ $thesis->student->name }}</b><small class="block text-slate-400">{{ $thesis->student->email }}</small></td><td class="max-w-xs pr-4">{{ $thesis->title }}</td><td><x-status-badge :status="$thesis->status"/></td><td>{{ $thesis->versions_count }}</td><td><a class="font-bold text-blue-600" href="{{ route('theses.show',$thesis) }}">Review →</a></td></tr>@empty<tr><td colspan="5" class="py-12 text-center text-slate-400">No assigned students found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $theses->links() }}</div></section></x-app-layout>
