<x-app-layout><div class="mx-auto max-w-4xl"><div class="flex items-center justify-between"><div><a class="text-sm font-bold text-blue-600" href="{{ route('theses.show',$thesis) }}">← Thesis</a><h1 class="mt-2 text-3xl font-black">Thesis conversation</h1><p class="text-slate-500">{{ $thesis->student->name }} & {{ $thesis->professor?->name }}</p></div></div><section class="card mt-6"><div class="max-h-[55vh] space-y-4 overflow-y-auto p-2">@forelse($thesis->messages as $message)<div class="flex {{ $message->sender_id===auth()->id() ? 'justify-end' : 'justify-start' }}"><div class="max-w-[80%] rounded-2xl px-4 py-3 {{ $message->sender_id===auth()->id() ? 'bg-blue-600 text-white' : 'bg-slate-100' }}"><p class="text-sm">{{ $message->message }}</p><small class="mt-1 block opacity-60">{{ $message->created_at->format('M j · g:i A') }}</small></div></div>@empty<p class="py-20 text-center text-slate-400">Start the conversation.</p>@endforelse</div><form method="POST" action="{{ route('chat.store',$thesis) }}" class="mt-5 flex gap-3 border-t pt-5">@csrf<input class="min-w-0 flex-1" name="message" required placeholder="Write a message..."><button class="btn-primary">Send</button></form></section></div></x-app-layout>
