@props(['label','value','hint'=>null,'color'=>'blue'])<div class="card"><div class="flex items-start justify-between"><div><p class="text-sm font-semibold text-slate-500">{{ $label }}</p><p class="mt-2 text-3xl font-black text-slate-900">{{ $value }}</p>@if($hint)<p class="mt-2 text-xs text-slate-500">{{ $hint }}</p>@endif</div><span class="size-3 rounded-full bg-{{ $color }}-500 shadow-[0_0_0_6px] shadow-{{ $color }}-100"></span></div></div>
